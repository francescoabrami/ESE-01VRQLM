\documentclass[../main.tex]{subfiles}

\graphicspath{{\subfix{../images/}}}



\begin{document}

%% BEGIN SUBFILE %%

\clearpage
\thispagestyle{empty}
\vspace*{7cm}
\section{Firmware}
\vspace*{1.5cm}


\subsection{Introduction}

Let’s now begin this second part of the course, in which we will introduce another aspect of embedded systems that are primarily related to the software side which interfaces with hardware different from what we have seen so far in computers, for which we have developed code.

In this section, and in the following ones, we will develop some code but with our focus still on the hardware that we have below us but nit forgetting the upper side looking at the operating system questione.

The following sectionn is what mainly fits between the two part, it fits between hardware and software and needs, to be comprehneded, the knoledge of what stays below ovvero l;architettura dei calcolatori e che ocsa cerca, non almano nel nostro caso, di ragigungere ovvero la possibilità di gestione delle risorse hardware di un sistema operativo avanzato.


%% LEZIONE VENERDÌ DA FARE SUBITO


Rules for software projects




C intro --> .c and .h files
source and header files.

Very clear role for what:

DONT want to ear the .h is a library file!
LIBRARY IS DIFFERENT --> bynary not text file!

Definitions and declarations
No definitions in .h file

Differenc ebtween def and dec. interface and implementation

I hace the func prot in the typialc io func in the stdio.h
a prototype is what the name says just the declarations --> somehwerre there is auncion [print f and it accepts the aprarmeters.
I dont know anything about the prink functions!


The listi of steps and instrucion are also needed but also are different from the two stuff.
Ideallu you only have def func.

BUT somebony has written down some funcs (libraries functions) sqrt anf others anf they have compiled the funcions the binary forms inside a binary file.
The binary file is al library.

The extension dependes on the OS --> defacto standards like UNIX suistem --> .a

and .so is a shared object library. 

On windows is .dll and what?? and .lib

Why library in binaruy form?

For i have a cost so i want to be paid or i closed form library
Open source and software and not


For firmware i have a set of.c and .h files.
I compiles the file and i have binary fiel exacutable!

Spurce to Binary files!
We need to be aware of what happensa insides --> hardware
Not able to run on our sys.

THE FLOW:

.c files --> preprocessor --> some text to other text --> .c file at the end
.o object file after the compiler

In windows the file type depebds of .xyz letters

CROSS COMPILATION

MISSED POINT -> linker fized strategy on PC

-- C EXAMPLE

Anither file that defines that func MANDATORY


%%% PROGRAM FOR FUTURE STUFF


\subsection{C programming language}


\subsection{Embedded code structure}

\subsection{STM32 Cube IDE}



























\end{document}